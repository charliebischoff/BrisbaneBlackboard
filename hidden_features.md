# these are features that have been hidden from the UI for simplicity but can be revived. they are probably slightly deprecated as well. we probably want to restore some in the settings menu

- line type choice in 'draw route'. it included screen lines too

- play, pause and rewind the current play. deprecated for now because all plays are one long continuous play for now. will be restored when we can add some kind of screen record function that when toggled, will start tracking the lines as part of the play

- playback speed also hidden for now

- saving plays also hidden for now. importantly, these saving and playback features are paused because the new whiteboard doesn't have real start or end points of plays, its just one very long string of actions, just like a real whiteboard
