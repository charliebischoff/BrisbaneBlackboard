1. set the default mode to be move & ball. hide the option to switch to other modes, we will ignore them for now. for all the buttons mentioned, remember to keep them big enough to click easily on an ipad touch screen.

2. instead of a sidebar, just have a minimal black bar at the top. it has two tabs, one for half court and one for full court. it also has a player roster icon flushed to the right (just 2 people abstract icon) that brings up our current roster selection view as a window that covers most of the screen. it also has a settings button on the right of the roster button that can be left blank for now. everything else from the existing sidebar will be hidden for now and moved to the settings menu at a later time.

3. in our roster window, instead of having buttons to add or remove players from the court, make each player bar a huge touch screen button that is highlighted when a player is on the court. remove the x button that removes a player from a roster, that will go to another menu in another update

4. only show the x most recent lines on the whiteboard. for now, set x as 3. erase all older lines after player or ball has been dragged and released

5. add an 'R' button at the top left of the screen below the top bar. when clicked, all lines should be erased.

6. when any player, ball or the R button is clicked/touched, make them expand and shrink in size really quickly just visually to show that they have been touched.
